#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int s;
    cout<<"Enter s:";
    cin>>s;
    string x=to_string(s);
    cout<<x.length();
    return 0;
}